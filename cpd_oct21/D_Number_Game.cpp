// lucky me 
#include <bits/stdc++.h>
#define all(arr) arr.begin(), arr.end()
#define Arr(T) vector<T> 
#define Pair(T) pair<int,int>
#define f first
#define s second

using namespace std;    
typedef long long ll;
int solve () {
    int n;
    cin >> n;
    Arr(int) arra(n);
    Arr(int) arrb(n);

    for (int i = 0; i < n; i ++) {
        cin >> arra[i] >> arrb[i];
    }


    Arr(Pair(int)) dp(n + 1);

    for (int i = 0; i < n; i ++) {
        
    }




}

int main() {
    cout << solve() << endl;
}